
Migration order:

1. Users
  - users.sql
  - user-url-slug-generation.js
  - user-url-unique-generation.js

2. Topics
  - topics.sql
  - topic-title-markdown-generation.js
  - topic-url-slug-generation.js
  - topic-url-unique-generation.js

3. Posts
  - posts.sql
  - post-content-markdown-generation.js

4. categories.sql

5. discussions.sql

6. announcements.sql

x. bookmarks.sql

x. permissions.sql

x. subscriptions.sql

x. topic-view-times.sql

x. cleanup.sql (set not nulls and uniques for items that couldn't be set that way during migration)

x. indexes.sql


new table: userLogs
  CREATE TABLE `originaltrilogy`.`userLogs` (
  `id` INT NOT NULL,
  `userID` INT NOT NULL,
  `action` VARCHAR(45) NOT NULL,
  `IP` VARCHAR(45) NOT NULL,
  `time` DATETIME NOT NULL,
  `timestamp` TIMESTAMP NOT NULL,
  PRIMARY KEY (`id`));


new table: postHistory
  CREATE TABLE `originaltrilogy`.`postHistory` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `postID` INT NOT NULL,
  `editorID` INT NOT NULL,
  `editReason` VARCHAR(500) NULL,
  `markdown` LONGTEXT NOT NULL,
  `time` DATETIME NOT NULL,
  `timestamp` TIMESTAMP NULL,
  PRIMARY KEY (`id`));
