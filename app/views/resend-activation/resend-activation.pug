header
  section.summary
    h1 Resend Activation E-mail

if local.error
  section.notice.error
    <svg class="icon error"><use xlink:href="themes/#{session.themePath.images}/images/symbols.svg#icon-error"></use></svg>
    p !{local.error.message}

section.content
  p If you didn't receive your activation e-mail, enter your e-mail address below and we'll send it again.

section.form.resend-activation
  form(action="resend-activation/action/submit" method="post" novalidate)
    ul.data
      li.email.text.required
        label(for="email") E-mail
        input(id="email" name="email" type="email" value=form.email required)
    ul.actions
      li.primary
        input(name="formAction" type="submit" value="Resend activation e-mail")
