h1 Lock a Post
    
section.post-lock-form
  if lock && lock.message
    p.message
      = lock.message
  form#post-lock-form(action="post/#{url.post}/action/lockForm" method="post" novalidate)
    .data
      input(name="forwardToUrl" type="hidden" value="#{form.forwardToUrl}")
      ul
        li.lock-reason.textarea
          label(for="reason") Why are you locking this post?
          p.label-note Optional
          textarea(id="reason" name="reason") #{form.reason}
        //- li.notify.checkbox
        //-   input(id="notify" name="notify" type="checkbox" value="true" checked=form.notify)
        //-   label(for="notify") Notify the author
    .actions
      ul
        li.primary.report
          input(name="formAction" type="submit" value="Lock this post")
        li.secondary.cancel
          a(href="#{request.headers.referer}" title="Return to the page you came from.") Cancel and go back

!= include.post
