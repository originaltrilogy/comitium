section.summary
  h1 Delete an Announcement
  p Deleting an announcement places it in the <a href="discussion/Trash/id/1">Trash</a> and removes it from any discussions in which it was posted. It can be recovered from the trash if necessary.

if trash && trash.message
  section.notice.error
    p !{trash.message}

section.form.topic-trash
  form#topic-trash-form(action="announcement/id/#{url.id}/action/trashForm" method="post" novalidate)
    .data
      input(name="forwardToUrl" type="hidden" value="#{form.forwardToUrl}")
      //- ul
      //-   li.notify.checkbox
      //-     input(id="notify" name="notify" type="checkbox" value="true" checked=form.notify)
      //-     label(for="notify") Notify the author
    .actions
      ul
        li.primary.report
          input(name="formAction" type="submit" value="Delete this announcement")
        li.secondary.cancel
          a(href="#{request.headers.referer}" title="Return to the page you came from.") Cancel and go back
