header
  section.summary
    h1 User Cleanup: !{user.username}

section.topics
  if topics
    p Clicking the button below will move all topics started by this user to the Trash.
    p <strong>This can't be undone without moving the topics back manually.</strong>
    form(method="post" action="user/action/cleanupForm/id/" + user.id)
      input(type="hidden" name="userID" value=url.id)
      ul.actions
        li.primary
          input(type="submit" value="Delete all user topics")
    h2 #{topics.length} Topics
    each topic in topics
      article(class="topic")
        section.description
          h3
            a(href="topic/" + topic.url + "/id/" + topic.id) !{topic.title_html}
          time
            = topic.post_date_formatted
  else
    p This user doesn't have any public topics.
